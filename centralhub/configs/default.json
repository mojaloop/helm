{{- $centralledgerHost := printf "%s-%s" .Release.Name .Values.centralledger.nameOverride -}}
{
    "PORT": "{{ .Values.service.internalPort }}",
    "HOSTNAME": "http://{{ .Values.ingress.hosts | first }}",
    "API_URL": "http://{{ $centralledgerHost }}:{{ .Values.centralledger.containers.api.service.ports.api.internalPort }}",
    "ADMIN_API_URL": "http://{{ $centralledgerHost }}:{{ .Values.centralledger.containers.admin.service.ports.api.internalPort }}"
}