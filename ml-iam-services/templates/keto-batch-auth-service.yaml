{{- if .Values.ketoBatchAuth.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-iam-services.fullname" . }}-keto-batch-auth
  labels:
    {{- include "ml-iam-services.labels" . | nindent 4 }}
    app.kubernetes.io/component: keto-batch-auth
spec:
  type: {{ .Values.ketoBatchAuth.service.type }}
  ports:
    - port: {{ .Values.ketoBatchAuth.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ml-iam-services.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: keto-batch-auth
{{- end }}
