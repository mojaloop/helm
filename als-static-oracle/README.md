# Static-Oracle

Static-Oracle is an Oracle for the Account Lookup Service.


## Dependencies

See `example_dependencies.yaml` for a simple example of installing and using the chart
with base dependencies.