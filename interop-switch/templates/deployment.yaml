apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "interop-switch.fullname" . }}
  labels:
    app: {{ template "interop-switch.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "interop-switch.name" . }}
        chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.image.imagePullSecrets }}
          imagePullSecrets:
{{ toYaml .Values.image.imagePullSecrets | indent 10 }}
          {{- end }}
          ports:
            - containerPort: {{ .Values.service.internalPort }}
          env:
            - name: proxy-host
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: proxy.host
            - name: account-lookup-service.host
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: account-lookup-service.host
            - name: account-lookup-service.port
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: account-lookup-service.port
            - name: central-services-host
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: central-services.host
            - name: central-ledger.host
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: central-ledger.host
            - name: central-ledger.port
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: central-ledger.port
            - name: metrics.reporter.kafka.broker
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: metrics.reporter.kafka.broker
            - name: metrics.reporter.environment
              valueFrom:
                configMapKeyRef:
                  name: {{ template "interop-switch.fullname" . }}
                  key: metrics.reporter.environment