apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "centralledger-handler-bulk-transfer-prepare.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "centralledger-handler-bulk-transfer-prepare.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.Version }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
  annotations:
    kubernetes.io/service-account.name: {{ include "centralledger-handler-bulk-transfer-prepare.fullname" . }}
