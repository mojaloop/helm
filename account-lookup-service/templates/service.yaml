apiVersion: v1
kind: Service
metadata:
  name: {{ include "account-lookup-service.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "account-lookup-service.name" . }}
    helm.sh/chart: {{ include "account-lookup-service.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "account-lookup-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
