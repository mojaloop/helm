account_lookup_service:
  # Don't need this - supplemented in main deployment
  enabled: false

auth_service:
  enabled: true

endpoint_registry:
  # Don't need this - supplemented in main deployment
  enabled: false

oracle_consent:
  enabled: true

thirdparty_api_adapter:
  enabled: true

transaction_requests_service:
  enabled: true