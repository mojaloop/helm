apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "routing.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "routing.name" . }}
    helm.sh/chart: {{ include "routing.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "routing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
