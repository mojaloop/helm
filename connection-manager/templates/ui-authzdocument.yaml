{{- if .Values.gateway.enabled }}
{{ include "mojaloop-common.authzdocument" (dict "context" . "backend" (printf "%s-ui" (include "connection-manager.fullname" .)) "authz" .Values.ui.authz) }}
{{- end }}
