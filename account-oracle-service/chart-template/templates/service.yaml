apiVersion: v1
kind: Service
metadata:
  name: {{ include "als-oracle-template.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "als-oracle-template.name" . }}
    helm.sh/chart: {{ include "als-oracle-template.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-api
      protocol: TCP
      name: http-api
  selector:
    app.kubernetes.io/name: {{ include "als-oracle-template.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
