apiVersion: v1
kind: Service
metadata:
  name: {{ include "inter-scheme-gateway.fullname" . }}
  labels:
    {{- include "inter-scheme-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inter-scheme-gateway.selectorLabels" . | nindent 4 }}
